# What the bundled interpreter gets: every runtime dependency of OCR Snap
# except Paddle itself, which the app installs on first run (CPU or CUDA
# build, chosen for the machine). packaging/constraints-bundle.txt pins the
# whole tree to the versions the dev venv runs.
#
# paddlepaddle is listed so its pure dependencies resolve together with the
# rest; the build uninstalls it again (build.py: install_dependencies).
PyQt6==6.10.2
numpy==2.4.2
pillow==12.1.1
requests==2.32.5
python-dotenv==1.2.1
QtAwesome==1.4.2
paddleocr==3.4.0
paddlex==3.4.1
paddlepaddle==3.3.0
# paddle.utils.cpp_extension imports setuptools when paddle loads, and the
# bundled interpreter ships pip only.
setuptools==80.9.0
